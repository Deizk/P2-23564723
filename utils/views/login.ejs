//NuevoRegisto.F

<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  

</head>
<body>
    <!-- partial:index.partial.html -->
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <!-- Design by foolishdeveloper.com -->
      <title>Login</title>

      <link rel="preconnect" href="https://fonts.gstatic.com">
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
      <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
      <!-- Fuentes -->
      <link rel="preconnect" href="https://fonts.googleapis.com">
      <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
      <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">

      <!--Stylesheet-->
      <style media="screen">
          *,
          *:before,
          *:after{
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }
        body{
            background-color: #080710;
        }
        .background{
            width: 550px;
            height: 70vh;
            position: absolute;
            transform: translate(-50%,-50%);
            left: 50%;
            top: 50%;
        }
        .background .shape{
            height: 200px;
            width: 200px;
            position: absolute;
            border-radius: 50%;
        }
        .shape:first-child{
            background: linear-gradient(
                #23a2f6,
                #18ad24
            );
            left: -80px;
            top: -80px;
        }
        .shape:last-child{
            background: linear-gradient(
                to right,
                #bd2fff,
                #24f019
            );
            right: -30px;
            bottom: -80px;
        }
        form{
            height:auto;
            width: 400px;
            background-color: rgba(255,255,255,0.13);
            position: absolute;
            transform: translate(-50%,-50%);
            top: 50%;
            left: 50%;
            border-radius: 10px;
            backdrop-filter: blur(10px);
            border: 2px solid rgba(255,255,255,0.1);
            box-shadow: 0 0 40px rgba(8,7,16,0.6);
            padding: 50px 35px;
        }
        form{
            font-family: 'Poppins',sans-serif;
            color: #ffffff;
            letter-spacing: 0.5px;
            outline: none;
            border: none;
        }
        form h4{
            font-family: "Bebas Neue", sans-serif;;
            font-size: 40px;
            font-weight: 500;
            line-height: 42px;
            text-align: center;
        }

        label{
            display: block;
            margin-top: 30px;
            font-size: 16px;
            font-weight: 500;
        }
        input{
            display: block;
            height: 50px;
            width: 100%;
            background-color: rgba(255,255,255,0.07);
            border-radius: 3px;
            padding: 0 10px;
            margin-top: 8px;
            font-size: 14px;
            font-weight: 300;
        }
        ::placeholder{
            color: #e5e5e5;
        }
        .button-login{
            display: block;
            text-decoration: none;
            margin: 50px auto 0;
            width: 12em;
            text-align: center;
            background-color: rgba(99, 253, 77, 1);
            color: #080710;
            padding: 7px 0;
            font-size: 18px;
            font-weight: 600;
            border-radius: 5px;
            cursor: pointer;
        }
        .social{
            display: flex;
            justify-content: center;
            align-items:center;
            gap:2rem;
            margin-top: 30px;
            display: flex;
        }
        .social div{
          background: red;
          width: 150px;
          border-radius: 3px;
          padding: 5px 10px 10px 5px;
          background-color: rgba(255,255,255,0.27);
          color: #eaf0fb;
          text-align: center;
      }
      .social div:hover{
          background-color: rgba(255,255,255,0.47);
      }
      .social .fb{
          margin-left: 25px;
      }
      .social i{
          margin-right: 4px;
      }

  </style>
</head>
<body>
    <div class="background">
        <div class="shape"></div>
        <div class="shape"></div>
    </div>
    <form id="form" class="formulario">
        <h4>Login</h4>

        <label for="correo">Correo</label>
        <input type="email" placeholder="Ingrese correo electronico" id="usuario" required>

        <label for="password">Contraseña</label>
        <input type="password" placeholder="Ingresa tu clave de acceso" id="password" required>
        
        <a class="button-login" href="/home">Acceder</button>

        </form>
    </body>
    </html>
    <!-- partial -->
    <script type="text/javascript">
        const form= document.querySelector("#form");

        form.addEventListener("submit",e=>{
            try{
                let u,p,c;
    e.preventDefault(); // Previene el envío del formulario
      // Obtiene los valores de los campos de entrada
    const usuario = document.querySelector("#usuario").value;
    const password = document.querySelector("#password").value;
    const cargo = document.querySelector("#cargo").value;

    localStorage.setItem(`usuario`,usuario)
    localStorage.setItem(`password`,password);
    localStorage.setItem(`cargo`,cargo);
    u = localStorage.getItem('usuario');
    p = localStorage.getItem('password');
    c = localStorage.getItem('cargo');

    alert(`Registro : "Registro Exitoso"
        usuario : ${u}
        password: ${p}
        cargo: ${c}`);  
}catch(e){

    console.error(e.message);
    alert(`Error : ${e.message}`);
}


});

</script>

</body>
</html>
